/* 
1- at first glance threads and processes are look similar,
in both we execute some code at the same time.
1- with threads, we have a function that we want to run in parallel.
with processes, we have a forked process that we want to run in parallel.
to be able to understand this I have to watch processes plazlist.
 */